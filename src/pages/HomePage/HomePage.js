import React from 'react';
import './HomePage.css';
import Header from '../../components/Header/Header';

const HomePage = () => {
    return (
        <header>
            <Header></Header>
        </header>
    );
};

export default HomePage;